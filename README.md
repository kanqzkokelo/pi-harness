# pi-harness — Controlled-Branch State-Graph (Pi fork target)
MVP: state graph + T0 test construction + deterministic verifier + same-parent beam (≤3, ≤2x) + Tree-sitter graph + git/Docker isolation.
See HARNESS_SPEC.md §12. Stack: TypeScript, sqlite, git worktree, Docker.
