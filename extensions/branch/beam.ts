// P0->T0->P1 (A/B/C same parent S0)->select->P2 iff no branch meets ACCEPT.
// TODO: wire pi agent loop + docker per branch (max 3 containers).
import {STRATS} from './promptsABC.ts';
console.log('beam stub',Object.keys(STRATS));
